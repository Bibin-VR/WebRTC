{
  "name": "webrtc-remote",
  "version": "1.0.0",
  "description": "Zero-auth remote screen sharing via WebRTC + Firebase — works across the internet",
  "bin": {
    "webrtc-remote": "./bin/cli.js"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "node bin/cli.js start",
    "monitor": "node bin/cli.js monitor"
  },
  "dependencies": {
    "express": "^4.18.0",
    "open": "^9.1.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
